🎨 Art Gallery Management System
Серверное приложение для управления арт-галереей. Система позволяет управлять пользователями, художниками, картинами и выставками, обеспечивая взаимодействие между ними.

Примечание для проверки: В репозиторий намеренно включен файл .env для быстрого запуска и тестирования функционала без дополнительной настройки окружения.

📋 Описание проекта
Проект представляет собой REST API, построенный на архитектуре MVC. Основные сущности:

Users & Roles: Система аутентификации с ролями (User, Moderator, Admin).

Artists: База данных художников.

Pictures: Картины с привязкой к автору и выставкам.

Exhibitions: Управление выставками.

При первом запуске база данных автоматически наполняется начальными ролями (user, moderator, admin) благодаря скрипту seeders/initialData.js.

🛠 Технологический стек
Node.js — среда выполнения.

Express — веб-фреймворк.

MongoDB & Mongoose — база данных и ODM для моделирования данных.

CORS — настройка доступа для фронтенда.

🚀 Установка и настройка
Так как папка node_modules исключена из репозитория, перед запуском необходимо установить зависимости.

Клонируйте репозиторий (или распакуйте архив):

Bash

git clone https://github.com/Clawdovik/Art-Gallery-MongoDB-
cd trade-app
Установите зависимости: Выполните команду в корневой папке проекта:

Bash

npm install
Настройка окружения: Файл .env уже присутствует в корне проекта и содержит необходимые настройки (порт сервера и строку подключения к MongoDB). Дополнительная настройка не требуется.

▶️ Запуск приложения
Есть два способа запустить сервер:

Способ 1: Через консоль (рекомендуемый)
Bash

node server.js
Способ 2: Через BAT-файл (Windows)
Просто запустите файл start.server.bat из корневой папки.

После успешного запуска вы увидите в консоли сообщения:

Plaintext

Connected to the database!
Server is running on port 8080.
📂 Структура проекта
Plaintext

trade-app/
├── app/
│   ├── config/         # Конфигурация БД
│   ├── models/         # Схемы Mongoose (User, Artist, Picture, Exhibition)
│   └── ...
├── public/             # Статические файлы (Frontend)
├── seeders/            # Скрипты для начального заполнения БД
├── server.js           # Точка входа приложения
└── .env                # Переменные окружения